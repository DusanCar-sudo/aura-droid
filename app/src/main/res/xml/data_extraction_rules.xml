<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup/transfer rules.

  allowBackup="false" already blocks cloud backup, but device-to-device
  transfer is a separate channel governed by this file. The app holds a
  pairing token that grants full control of the paired desktop agent, so
  neither channel should carry it: a token cloned onto a second device is a
  second thing that can drive your machine, and revoking it is not something
  the user would think to do.

  Re-pairing takes a few seconds and is the correct trade.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>

    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
